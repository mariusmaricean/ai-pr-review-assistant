# Screenshots To Capture

Add demo assets here as the product stabilizes:

- `swagger-api.png`: Swagger overview with webhook, retrieval, and metrics endpoints.
- `metrics-endpoint.png`: Successful `GET /metrics/reviews` response.
- `inline-pr-comments.png`: AI inline comments on a GitHub pull request.
- `worker-logs.png`: Celery worker processing a queued review job.
- `review-summary.gif`: Optional short demo from webhook trigger to GitHub comment.
